{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "QuickLog-Solo Settings File Schema",
  "description": "Schema for the settings.json file used in QuickLog-Solo backups.",
  "definitions": {
    "animationId": {
      "type": "string",
      "maxLength": 50
    }
  },
  "type": "object",
  "required": ["app", "kind", "version", "entries"],
  "additionalProperties": false,
  "properties": {
    "app": {
      "type": "string",
      "const": "QuickLog-Solo",
      "description": "Application identifier."
    },
    "kind": {
      "type": "string",
      "const": "QuickLogSolo/Settings",
      "description": "Data kind identifier."
    },
    "version": {
      "type": "string",
      "const": "1.0",
      "description": "Schema version."
    },
    "entries": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["key", "value"],
        "additionalProperties": false,
        "properties": {
          "key": {
            "type": "string",
            "enum": [
              "theme",
              "font",
              "defaultAnimation",
              "language",
              "reportSettings",
                "businessDays",
              "alarms"
            ],
            "description": "The unique key for the setting."
          },
          "value": {
            "description": "The value associated with the setting key."
          }
        },
        "allOf": [
          {
            "if": { "properties": { "key": { "const": "theme" } } },
            "then": {
              "properties": {
                "value": { "type": "string", "enum": ["system", "light", "dark"] }
              }
            }
          },
          {
            "if": { "properties": { "key": { "const": "font" } } },
            "then": {
              "properties": {
                "value": { "type": "string", "maxLength": 200 }
              }
            }
          },
          {
            "if": { "properties": { "key": { "const": "defaultAnimation" } } },
            "then": {
              "properties": {
                "value": { "$ref": "#/definitions/animationId" }
              }
            }
          },
          {
            "if": { "properties": { "key": { "const": "language" } } },
            "then": {
              "properties": {
                "value": {
                  "type": "string",
                  "enum": ["auto", "ja", "en", "de", "es", "fr", "pt", "ko", "zh"]
                }
              }
            }
          },
          {
            "if": { "properties": { "key": { "const": "reportSettings" } } },
            "then": {
              "properties": {
                "value": {
                  "type": "object",
                  "required": ["format", "emoji", "endTime", "duration", "adjust"],
                  "additionalProperties": false,
                  "properties": {
                    "format": {
                      "type": "string",
                      "enum": ["markdown", "wiki", "html", "csv", "text-plain", "text-table"]
                    },
                    "emoji": { "type": "string", "enum": ["keep", "remove"] },
                    "endTime": { "type": "string", "enum": ["none", "show"] },
                    "duration": { "type": "string", "enum": ["none", "right", "bottom"] },
                    "adjust": { "type": "string", "enum": ["none", "5", "10", "15", "30", "60"] }
                  }
                }
              }
            }
          },
          {
            "if": { "properties": { "key": { "const": "businessDays" } } },
            "then": {
              "properties": {
                "value": {
                  "type": "array",
                  "items": { "type": "integer", "minimum": 0, "maximum": 6 },
                  "minItems": 1,
                  "maxItems": 7,
                  "uniqueItems": true
                }
              }
            }
          },
          {
            "if": { "properties": { "key": { "const": "alarms" } } },
            "then": {
              "properties": {
                "value": {
                  "type": "array",
                  "items": {
                    "type": "object",
                    "required": [
                      "enabled", "time", "message", "action", "actionCategory", "requireConfirmation",
                      "type", "daysOfWeek", "dayOfMonth", "daysBeforeEnd", "holidayAdjustment"
                    ],
                    "additionalProperties": false,
                    "properties": {
                      "id": { "type": "integer" },
                      "enabled": { "type": "boolean" },
                      "time": { "type": "string", "pattern": "^([01]\\d|2[0-3]):([0-5]\\d)$" },
                      "message": { "type": "string", "maxLength": 200 },
                      "action": { "type": "string", "enum": ["none", "stop", "pause", "start"] },
                      "actionCategory": { "type": "string", "maxLength": 100 },
                      "requireConfirmation": { "type": "boolean" },
                      "type": { "type": "string", "enum": ["daily_business", "weekly", "monthly_date", "monthly_end_relative"] },
                      "daysOfWeek": {
                        "type": "array",
                        "items": { "type": "integer", "minimum": 0, "maximum": 6 },
                        "uniqueItems": true
                      },
                      "dayOfMonth": { "type": "integer", "minimum": 1, "maximum": 31 },
                      "daysBeforeEnd": { "type": "integer", "minimum": 0, "maximum": 31 },
                      "holidayAdjustment": { "type": "string", "enum": ["none", "prev_business_day", "next_business_day", "skip"] }
                    }
                  }
                }
              }
            }
          }
        ]
      }
    }
  }
}
