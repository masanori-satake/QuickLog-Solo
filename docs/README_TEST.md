# QuickLog-Solo: テスト計画・ケース定義書

本プロジェクトでは、コアロジックおよびデータアクセス層の品質を担保するため、Jest による自動テストを実施しています。

## 1. テスト環境・方針

-   **テストランナー:** Jest
-   **エミュレーション:** jsdom (ブラウザ環境), fake-indexeddb (IndexedDB)
-   **テスト対象:** `js/logic.js` (ビジネスロジック), `js/db.js` (データアクセス), `js/backup.js` (バックアップ), `js/utils.js` (ユーティリティ) 等
-   **実行タイミング:** 開発時の手動実行、およびコミット時の pre-commit フックによる強制実行。

---

## 2. テストケース一覧

### 2.0. E2E・ビジュアルテスト (`tests/*.spec.js`)

| カテゴリ | テストファイル | ケース概要 | 期待される結果 |
| :--- | :--- | :--- | :--- |
| 正常系 | `ui_settings.spec.js` | 一般設定・アラーム設定の永続化 | 言語、テーマ、フォント、背景アニメーション、およびアラームの各設定値がリロード後も維持される |
| 正常系 | `category_editor_features.spec.js` | カテゴリエディタ拡張機能検証 | FullHDレイアウト、複数選択（Ctrl/Shift）、一括削除、一括プロパティ更新（テーマ・アニメーション）が正しく機能する |
| 正常系 | `category_editor_multi_tags.spec.js` | カテゴリエディタ 複数選択時タグ編集 | 複数選択時のアクションボタン非表示、ラベルの動的変更、共通タグの抽出・ソート、一括追加・削除が正しく機能する |
| 正常系 | `category_editor_undo_redo.spec.js` | カテゴリエディタ Undo/Redo | 追加、削除、編集、プロパティ変更、全消去などの操作が Undo/Redo でき、履歴制限（50件）が正しく機能する |
| 正常系 | `category_editor_tag_box.spec.js` | カテゴリエディタ タグボックス | 全カテゴリから抽出されたユニークなタグの表示、ドラッグ＆ドロップによるタグ追加、重複排除、および自動更新が正しく機能する |
| 正常系 | `category_editor_tag_replace.spec.js` | カテゴリエディタ タグ置換 | タグ置換モーダルの開閉、全カテゴリを対象としたタグの置換・削除、ドラッグ＆ドロップ、およびモーダル内Undo/Redoが正しく機能する |
| 正常系 | `analysis.spec.js` | 日報生成・タグ集計の正確性 | 各フォーマット（Markdown/CSV等）での日報プレビュー、時間丸め、およびタグごとの時間集計が正確に行われる |
| 正常系 | `data_io.spec.js` | カテゴリ・履歴の入出力整合性 | カテゴリのエクスポート(NDJSON)がスキーマに準拠し、履歴のエクスポート(CSV)がDB内容と一致する。破損データへの対応も検証。 |
| 正常系/堅牢性 | `history_import.spec.js` | 履歴CSVインポートの堅牢性 | 未来の時刻や重複データのスキップ、不正な行の検知とユーザーへの通知が正しく行われる |
| 正常系 | `maintenance.spec.js` | メンテナンス機能（初期化） | ログの削除、設定の初期化、全データの初期化が意図通りに機能し、 IndexedDB がクリーンアップされる |
| 正常系 | `sync.spec.js` | 複数タブ間の状態同期検証 | 設定変更やタスク開始が別タブに即座に反映され、メンテナンス操作時に全タブが同期する |
| 正常系 | `theme.spec.js` | テーマ切り替え・コントラスト検証 | ダーク・ライトモードの背景色・文字色が適切に設定され、コントラストが確保されている |
| 正常系 | `animation_evaluation.spec.js` | アニメーション品質評価 | 全アニメーションが応答性、密度、変化量の基準を満たしている |
| 正常系 | `animation_verification.spec.js` | アニメーション描画の基本検証 | デフォルトのアニメーション（Digital Rain）が正しく描画され、Canvasに内容がある |
| 正常系 | `dev_only_exclusion.spec.js` | 開発専用モジュールの露出検証 | 開発環境において `devOnly: true` のモジュールが選択肢に表示され、動作する |
| 正常系 | `test_pattern_verification.spec.js` | 重ね合わせ透過度検証 | 動作確認用パターンが、UI要素の背景色（透過度）によって遮られていない |
| 正常系 | `preview_repro.spec.js` | ランディングページ プレビュー検証 | ランディングページの iframe 内でアニメーションが正しく再生される |
| 正常系 | `guide_verification.spec.js` | クイックガイド検証 | ガイドに表示されるキャプチャ画像が存在し、各言語で正しく表示されている |

### 2.1. ビジネスロジック (`js/logic.js`)

| カテゴリ | テスト関数 | ケース概要 | 期待される結果 |
| :--- | :--- | :--- | :--- |
| 正常系 | `formatDuration` | ミリ秒を時間・分・秒に変換する | 時・分・秒の数値を持つオブジェクトが返り、`toString()` でパディング済みの HH:MM:SS 形式が得られる |
| 正常系 | `formatLogDuration` | 履歴表示用の丸め処理検証 | 30秒/30分単位での丸め、および10分未満の場合のスペース挿入が正しく行われる |
| 正常系 | `startTaskLogic` | 新規タスクを開始する | 新しいタスクオブジェクトが生成され、DBに保存される |
| 正常系 | `startTaskLogic` | 実行中と同じカテゴリを開始する | 何もせず現在のタスクをそのまま返す（二重開始防止） |
| 正常系 | `stopTaskLogic` | アクティブなタスクを終了する | `endTime` が記録され、戻り値が `null` になる |
| 正常系 | `pauseTaskLogic` | 作業を一時停止する | 現在のタスクが終了し、`__IDLE__` カテゴリのタスクが開始される |
| 正常系 | `calculateTagAggregation` | タグごとの集計ロジック検証 | タグの重複排除、および待機/手動終了ログを除外した正確な合計時間が計算される |
| 正常系 | `generateReport` | レポート生成ロジック検証 | 指定されたフォーマット（MD/CSV等）や時間調整（5分単位等）が正しく適用される |
| 正常系 | `stripEmojis` / `getVisualWidth` / `visualPadEnd` | テキスト処理 utilities | 絵文字除去、表示幅計算、および表示幅に合わせたパディング処理が正確に行われる |

### 2.2. データベース操作 (`js/db.js`)

| カテゴリ | テスト関数 | ケース概要 | 期待される結果 |
| :--- | :--- | :--- | :--- |
| 正常系 | `openDatabase` | データベースとストアの作成 | `logs`, `categories`, `settings` の各ストアが存在する |
| 正常系 | `dbAdd` / `dbGet` | データの追加と取得 | 保存したオブジェクトが正確に取得できる |
| 正常系 | `dbGetAll` | 全件取得 | ストア内のすべてのレコードが配列で取得できる |
| 正常系 | `dbPut` | データの更新 | 既存のデータが新しい値で上書きされる |
| 正常系 | `dbDelete` | データの削除 | 指定したキーのレコードが削除され、取得できなくなる |
| 正常系 | `dbCount` | レコード件数取得 | 指定したストアの全レコード数が正確に返る |
| 正常系 | `dbImportCategories` | カテゴリインポート | 上書き/追加モード、および不正な色の自動補正が正しく機能する |
| 正常系 | `initDB` | 初期データセットアップ | 初回起動時にデフォルトのカテゴリ群が自動生成される |
| 整合性 | `initDB` | 複数タスクの不整合修復 | 複数の未終了タスクがある場合、最新以外を自動終了させる |
| 正常系 | `closeDatabase` | データベースの切断 | 接続を安全に終了し、テスト間の干渉を防止する |

### 2.3. バックアップ・ユーティリティ・i18n

| カテゴリ | テストファイル | ケース概要 | 期待される結果 |
| :--- | :--- | :--- | :--- |
| 正常系 | `backup.test.js` | `BackupManager` 状態遷移検証 | ディレクトリハンドルの保持、アクセス権限の有無による状態遷移が仕様通りに行われる |
| 正常系 | `backup.test.js` | 手動バックアップ検証 | バックアップ実行時にファイルへの書き出しと最終バックアップ時刻の更新が行われる |
| 異常系 | `abnormal_backup.test.js` | ロックされたディレクトリのハンドリング | ディレクトリがロックされている場合やアクセス権限がない場合に、適切にエラー（`backup-err-locked`）が返されることの検証 |
| 正常系 | `utils.test.js` | HTML/CSV エスケープ検証 | 特殊文字が正しく変換され、XSSやCSV崩れを防止できる |
| 正常系 | `utils.test.js` | 入力値バリデーション検証 | カテゴリ名（長さ・重複・禁止名）や色の妥当性が正確に判定される |
| 正常系 | `schema_compliance.test.js` | スキーマ準拠性検証 | カテゴリ、履歴、設定のデータが定義された JSON スキーマに正しく準拠しているか |
| 整合性 | `i18n_coverage.test.js` | 翻訳キーの網羅性検証 | 全言語において `en` と同等のキーが定義されており、欠落がない |
| 正常系 | `background.test.js` | アラーム発火時の状態遷移検証 | 現在の状態とアラームのアクションを比較し、状態が変わらない場合は通知と実行をスキップする |
| 正常系 | `background.test.js` | 通知のみのアラーム検証 | アクションが 'none' の場合は、状態に関わらず常に通知が表示される |
| 正常系 | `background.test.js` | テストアラームの動作検証 | テストアラームが状態に関わらず常に通知を表示し、正常に機能する |
| 正常系 | `background.test.js` | 確認が必要なアラームの検証 | `requireConfirmation` が有効な場合、即座にアクションを実行せず、通知ボタンのクリックを待つ |

---

## 3. ビジュアルテストの効率化

UIの確認や特定の状態（長時間経過など）のテストを容易にするため、URLパラメータによる状態注入をサポートしています。

-   **タスクの強制開始:** `?test_cat=[カテゴリ名]&test_elapsed=[経過ミリ秒]`
-   **一時停止状態の再現:** `?test_cat=__IDLE__&test_resumable=[再開カテゴリ名]`

例: `app.html?test_cat=🤝 会議&test_elapsed=3600000` (1時間経過した会議を表示)

## 4. テストの実行方法

### ユニットテスト
```bash
npm test
```

### E2Eテスト (Playwright)
※事前にローカルサーバー（Vite 等）を起動しておく必要があります（デフォルト: 8080ポート）。
`npm run test:e2e` コマンドは、実行前に `scripts/generate_animation_registry.py` を呼び出し、アニメーションレジストリが最新の状態であることを保証します。

Playwright テストは `playwright.config.js` で `baseURL` が `http://localhost:8080/projects/app/app.html` に設定されています。そのため、テストケース内では `page.goto('')` や `page.goto('?param=value')` のように相対パスでナビゲーションを行ってください。これにより、リポジトリルートに `index.html` が存在しない問題に起因するテスト失敗を防ぎます。

```bash
npm run test:e2e
```

### アニメーション評価テスト
個々のアニメーションが品質基準（初期応答性、継続性、密度、変化量）を満たしているかを検証します。
具体的には、以下のしきい値を基準としています：
- **密度:** 平均アクティブドット数が 10 以上であること (`MIN_AVERAGE_DOTS: 10`)。
- **変化量:** フレーム間のピクセル変化率が 0.01% 以上であること (`MIN_CHANGE_RATE: 0.0001`)。
これにより、静止画のような動きの少ないアニメーションや、描画エラーによる空白画面を検知します。
```bash
npm run test:animation-eval
```

すべてのテストがパスすることを確認してからコミットしてください。

---

## 5. 関連ドキュメント

- [製品仕様書 (spec.md)](spec.md)
- [開発者ガイド (README_DEV.md)](README_DEV.md)
- [背景アニメーション・モジュール仕様書 (animation_module_spec.md)](animation_module_spec.md)
- [AI エージェント指針 (AGENTS.md)](../AGENTS.md)
