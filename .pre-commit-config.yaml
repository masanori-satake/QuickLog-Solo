repos:
-   repo: local
    hooks:
    -   id: check-version
        name: check-version
        entry: python3 check_version.py
        language: python
        pass_filenames: false
        always_run: true
